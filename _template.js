function check() {
	// [TODO(generate with encode.js)]
}

load({
	check,
	id: -1, // [TODO(next challenge number)]
	name: "[TODO(challenge name, two random words)]",
	scenario: `
[TODO(representative HTML snippet, use %s for where the users input goes)]
`,
});
